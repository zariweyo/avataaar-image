name: avataaar_image
description: Flutter wrapper widget for Avataaars API allowing to generate and display avatar images.
version: 1.1.0
homepage: https://github.com/tomwyr/avataaar-image/

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg:
    git:
      url: git@github.com:tomwyr/flutter_svg.git
      ref: skip-null-mask

dev_dependencies:
  flutter_test:
    sdk: flutter
